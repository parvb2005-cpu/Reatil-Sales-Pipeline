# Reatil-Sales-Pipeline
Built a Retail Sales ETL Pipeline using Python, Pandas, NumPy, SQLite, and SQL. The project loads data from CSV files, cleans and merges datasets, calculates revenue, stores the processed data in a SQLite database, and generates business insights using SQL queries. It also includes an automated ETL pipeline with basic error handling.
